{
  "name": "cryptography",
  "version": "0.0.1",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "nyc -a node test/",
    "test": "VERBOSE=true nyc -a node test/",
    "test:slow": "VERBOSE=true RUNS=5000 nyc -a node ./test",
    "lint": "esw -w ./src ./test",
    "lint:fix": "eslint --fix ./src ./test"
  },
  "author": "Jascha Ehrenreich <jascha@jaeh.at>",
  "eslintConfig": {
    "extends": "standard",
    "rules": {
      "comma-dangle": ["error", "always-multiline"],
      "brace-style": ["error", "stroustrup"],
      "semi": ["error", "never"]
    }
  },
  "devDependencies": {
    "test": "git+http://git@localhost:7005/test"
  },
  "dependencies": {
    "bcrypt": "1.0.3",
    "types": "git+http://git@localhost:7005/types",
    "uuid": "3.2.1"
  }
}
